# Qdrant Vector Database Configuration
QDRANT_HOST=localhost
QDRANT_PORT=6333

# Optional: External services (if using cloud-hosted Qdrant)
# QDRANT_URL=http://your-qdrant-instance:6333
# QDRANT_API_KEY=your-api-key

# Optional: PostgreSQL Configuration (for future use)
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_DB=cv_analyzer
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=password

# Optional: MinIO Configuration (for future use)
# MINIO_ENDPOINT=http://localhost:9000
# MINIO_ACCESS_KEY=minioadmin
# MINIO_SECRET_KEY=minioadmin